server:
  address: 0.0.0.0
  port: 8080
  servlet:
    context-path: /App

spring:
  profiles:
    active: local
  application:
    name: Test2-BookReview

#  datasource:
#    url: ${DB_URL}
#    username: ${DB_USER}
#    password: ${DB_PASS}
#    driver-class-name: org.postgresql.Driver
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
